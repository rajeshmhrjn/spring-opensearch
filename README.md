# spring-opensearch
A learner project exploring the [OpenSearch Java client] from a Spring Boot application. It covers CRUD operations and a range of search query types (match, term, range, bool, fuzzy, wildcard, geo, etc.) against two OpenSearch indices: `products` and `shipments`.
